{
  "name": "synapse-ai",
  "version": "1.0.0",
  "description": "SynapseAI — AI-Powered Content Intelligence Platform using Local LLM",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["ai", "llm", "ollama", "react", "express", "full-stack"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
